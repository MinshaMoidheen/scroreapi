# Server Configuration
# Port on which the server will run (optional, defaults to 5031)
PORT=5050

# Node environment: "development" | "production" | "test" (optional)
NODE_ENV=development

# MongoDB Configuration
# Main MongoDB connection string (required)
# Examples:
#   Local: mongodb://localhost:27017
#   Atlas: mongodb+srv://username:password@cluster.mongodb.net/
MONGO_URL=mongodb://localhost:27017

# Local MongoDB URI for logs database (optional, defaults to mongodb://localhost:27017)
LOCAL_MONGO_URL=mongodb://localhost:27017

# JWT Configuration
# Secret key for encoding/decoding JWT access tokens (required)
# Generate a strong random secret key for production
JWT_ACCESS_SECRET=your-super-secret-access-token-key-change-in-production

# Secret key for encoding/decoding JWT refresh tokens (required)
# Generate a different strong random secret key for production
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-in-production

# JWT Token Expiry
# Access token expiry time (optional, uses ms format: e.g., "30m", "1h", "7d")
ACCESS_TOKEN_EXPIRY=30m

# Refresh token expiry time (optional, uses ms format: e.g., "7d", "30d")
REFRESH_TOKEN_EXPIRY=7d

# Logging Configuration
# Log level: "error" | "warn" | "info" | "debug" (optional, defaults to "info")
LOG_LEVEL=info

# Maximum size of a log file before rotation (optional, defaults to "20MB")
LOG_FILE_MAX_SIZE=20MB

# Maximum number of log files to keep (optional, defaults to "10")
LOG_FILE_MAX_FILES=10

# Directory where log files are stored (optional, defaults to "logs")
LOG_DIR=logs

# Superadmin Configuration
# Superadmin password (optional, defaults to "basil1")
# Change this in production!
SUPERADMIN_PASSWORD=basil1

# Superadmin email (optional, defaults to "basiljijiwork@gmail.com")
SUPERADMIN_MAIL=basiljijiwork@gmail.com

# Superadmin username (optional, defaults to "SUPERADMIN")
SUPERADMIN_USERNAME=SUPERADMIN

# Puppeteer Configuration (optional)
# Path to Chrome/Chromium executable for Puppeteer
# Only set if you need to use a custom Chrome installation
# PUPPETEER_EXECUTABLE_PATH=/path/to/chrome
# PUPPETEER_CHROMIUM_EXECUTABLE_PATH=/path/to/chromium
